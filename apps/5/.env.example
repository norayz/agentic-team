# Node Environment (development or production)
NODE_ENV=development

# Server Port
PORT=3000

# Database Path (inside container, mounted to ./db volume)
DB_PATH=/app/db/orders.db

# Logging Level (debug, info, warn, error)
LOG_LEVEL=info
